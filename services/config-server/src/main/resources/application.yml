server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native   

  cloud:
    config:
      server:
        native:
               search-locations: file:D:/Microservcies%20and%20CICD/Project%20Code/Student-team-beta/services/config-server/config-repo
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: ${SERVER_PORT:8083}

spring:
  datasource:
    url: jdbc:mysql://classroom-dev-db.cvwy4uckycwn.eu-west-1.rds.amazonaws.com:3306/booking_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: admin
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    table: flyway_schema_history_booking_service
